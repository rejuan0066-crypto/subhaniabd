// This component is no longer used - the receipt designer now uses a simplified settings-based approach.
// Kept as an empty export to prevent import errors if referenced elsewhere.
const DesignerCanvas = () => null;
DesignerCanvas.displayName = 'DesignerCanvas';
export default DesignerCanvas;
